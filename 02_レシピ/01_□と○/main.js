const figure = document.getElementById("figure")

figure.onclick = function () {
  figure.classList.toggle("rounded")
  // if ((figure.classList = "rounded)) {
  // figure.classList.toggle("tetora")
  // }

  //.classList.removeで、クラス削除なのかな
} //あー、△にするのがわからん。
